<%= form_for(@typeface) do |f| %>
  <% if @typeface.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typeface.errors.count, "error") %> prohibited this typeface from being saved:</h2>

      <ul>
      <% @typeface.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :familyname %><br>
    <%= f.text_field :familyname %>
  </div>
  <div class="field">
    <%= f.label :designer %><br>
    <%= f.text_field :designer %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
